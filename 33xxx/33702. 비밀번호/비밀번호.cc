#include <bits/stdc++.h>
using namespace std;
void mainCode() {
    int N; cin >> N;
    if(N%2==0) cout << "0";
    else  cout  << "8";
}
int main() {
    ios::sync_with_stdio(0);
    cin.tie(0);
    mainCode();
}

